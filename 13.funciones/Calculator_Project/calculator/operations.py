def addition(a, b):
            return a + b 
def subtraction(a, b):
        return a - b 
def multiply(a, b):
        return a * b 
def divide(a, b):
    if b == 0:
        raise ValueError("It is not possible to divide by 0")
    else: 
        return a / b     
